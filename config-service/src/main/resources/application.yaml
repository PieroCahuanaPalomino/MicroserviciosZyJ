server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: master-circuit
          uri: https://github.com/PieroCahuanaPalomino/MicroserviciosZyJ.git
          search-paths: config-data
  application:
    name: config-service        